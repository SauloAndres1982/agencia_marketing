import { connect } from "react-redux"

function Footer() {
    return(
        <footer>
            Footer
        </footer>
    )
}
const mapStateToProps=state=>({
})

export default connect(mapStateToProps, {

}) (Footer)